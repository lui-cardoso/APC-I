#include <stdio.h>

int main() {

    int inteiro;
    int negativo;
    int zeros;

    inteiro = 10;
    negativo = -5;
    zeros = 00004;

    printf("%i %i %i", inteiro, negativo, zeros);

    return 0;
}